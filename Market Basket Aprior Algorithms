#Importing necessary librarie
from mlxtend.preprocessing import TransactionEncoder
import pandas as pd
import numpy as np

# Assume that you have a crosstab to summarize the total entris of order history by each product and group by customer number called 'ct_data'
# For modelling and finding the relationship between products we need to be working with numerical values, so let's one hot encode the products
def encode(item_freq):
    res = 0
    if item_freq > 0:
        res = 1
    return res
    
basket_input = ct_data.applymap(encode)

